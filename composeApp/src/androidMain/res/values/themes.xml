<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Window theme. All real theming lives in Compose; the window only needs
         transparent, edge-to-edge system bars and a background matching the app. -->
    <style name="Theme.Studentbsuby" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
        <item name="android:enforceNavigationBarContrast" tools:targetApi="q">false</item>
        <item name="android:enforceStatusBarContrast" tools:targetApi="q">false</item>
    </style>

    <style name="Theme.Studentbsuby.NoActionBar" />

    <!-- Launch theme: the system splash screen (Android 12+) and its backport
         (androidx.core.splashscreen) show the BSU logo on the brand blue background
         until the first Compose frame; MainActivity switches to the app theme. -->
    <style name="Theme.Studentbsuby.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/blue</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenAnimationDuration">200</item>
        <item name="postSplashScreenTheme">@style/Theme.Studentbsuby.NoActionBar</item>
    </style>
</resources>
